# First way to import Library:

import calendar  # 'import calender' statement imports the whole calendar library in this particular python program

print(calendar.month(2024,3)) #calendar.month is used to print entire month
print(calendar.calendar(2022)) #calendar.calendar is used to print entire year

'''We can also write the above as:
   
   import calendar in c
   print(c.month(2024,3))
   print(c.calendar(2022)) - this helps is shortening the writing efforts of a coder '''